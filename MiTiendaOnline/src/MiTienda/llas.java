/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package MiTienda;

/**
 *
 * @author carcas
 */
/*public class llas {
    public static void llenarArchivo(ArrayList<Producto> listaProductos, String fichero) {
        try ( ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(fichero))) {
            oos.writeObject(listaProductos);
            System.out.println("Datos guardados correctamente en " + fichero);
        } catch (IOException e) {
            System.out.println("Error al guardar los datos en " + fichero);
        }
    }

    public static void visualizarLista(String fichero) {
        
    }

 for (Producto libro : listaProductos) {
            if (libro instanceof Libro) {
                listaLibro.add((Libro) libro);
            }
        }
    }
}
 */